# AWS CUR Wizard Configuration
# Paths for data normalization and Rill generation

# Input: Raw AWS CUR parquet files from dlt
INPUT_DATA_DIR=./data/aws_costs/cur_export_test_00001

# Input: Raw GCP billing parquet files from dlt
INPUT_DATA_DIR_GCP=./data/gcp_costs

# Output: Normalized parquet file
NORMALIZED_DATA_DIR=./data

# Rill project path (current directory)
RILL_PROJECT_PATH=.

# Optional: Customize cost column (default: line_item_unblended_cost)
# COST_COL=line_item_unblended_cost

# Optional: Dimension prefixes for dynamic canvases (comma-separated)
# DIM_PREFIXES=resource_tags_,product_

# Optional: Timeseries column (default: date extracted from identity_time_interval)
# TIMESERIES_COL=date

## ClickHouse Cloud Configuration (for production)
# Uncomment these to use ClickHouse instead of local parquet files

# 1. Set connector to clickhouse
# RILL_CONNECTOR=clickhouse

# 2. Add ClickHouse connection string
# connector.clickhouse.dsn=clickhouse://username:password@host:port/database?secure=true
# Example:
# CONNECTOR_CLICKHOUSE_DSN=clickhouse://default:your-password@xxxxx.europe-west4.gcp.clickhouse.cloud:8443/default?secure=true

## MotherDuck Configuration
# Uncomment these to use MotherDuck instead of local parquet files

# 1. Set connector to motherduck
# RILL_CONNECTOR="motherduck"

# 2. Add MotherDuck token (get from app.motherduck.com → Settings → Access Tokens)
# CONNECTOR_MOTHERDUCK_TOKEN="eyJ..."
